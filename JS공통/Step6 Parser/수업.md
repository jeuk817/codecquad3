# Stack
- 자료구조의 일종
- LIFO

# ADT
- Abstract Data Type ( = 자료구조 구현 클래스) ( 클래스는 프로퍼티와 메소드가 있는것)
- 어떠한 자료구조를 구현하기 위해 필요한 메소드들의 모음

## ADT Stack
```
class Stack{
        constructor()
        ,push(item){}
    ,pop(){}
    ,size(){}
    ,peek(){}
    ,isEmpty(){}
}
```

tree, binary tree, DOM 
BST, AVL, RB, 순회('구현암기' traversal) (전위순회, 중위순회, 후위순회)
불균형트리, 균형트리

그래프
- 이차원배열

그래프 (DFS: 깊이우선순회, BFS: 너비우선순회)

DFS는 stack을 이용해서 찾아간다. 재귀호출을 이용해서 순회할수도 있다.

